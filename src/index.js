import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';

ReactDOM.render(<App />, document.getElementById('root')); // the frist parameter is the component, and the second is the parameter we want to hook